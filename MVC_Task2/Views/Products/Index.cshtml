@model List<Product>

@* Link to the new external stylesheet inside wwwroot *@
<link rel="stylesheet" href="~/css/Products/index.css" asp-append-version="true" />

<a asp-action="Create" class="btn-create">
    <svg style="width:16px; height:16px; margin-right:6px;" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
    </svg>
    Create Product
</a>

<div class="table-container">
    <table class="styled-table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Price</th>
                <th>Stock</th>
                <th>Category</th>
                <th style="width: 80px;">Img</th>
                <th style="text-align: right; width: 160px;">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (Product n in Model)
            {
                <tr>
                    <td class="product-name">@n.Name</td>
                    <td class="price-col">$@n.Price</td>
                    <td><span class="badge-stock">@n.Stock units</span></td>
                    <td><span class="badge-category">@n.Category.Name</span></td>
                    <td>
                        <div class="img-thumbnail-wrapper">
                            <img src="@n.ImgPath" alt="@n.Name" onerror="this.style.display='none'; this.nextElementSibling.style.display='block';" />
                            <span class="img-fallback">No Img</span>
                        </div>
                    </td>
                    <td>
                        <div class="action-cell">
                            @* Action Edit Route Passing the record primary key context *@
                            <a asp-action="Edit" asp-route-id="@n.Id" class="btn-action btn-edit">
                                Edit
                            </a>

                            @* Action Delete Route passing parameter row id reference context context *@
                            <a asp-action="Delete" asp-route-id="@n.Id" class="btn-action btn-delete" onclick="return confirm('Are you sure you want to delete this product?');">
                                Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
